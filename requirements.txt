# CMC Strategy Engine - Dependencies
# Core (stdlib only - no deps for core logic)
# urllib, json, os, time, dataclasses are all stdlib

# Optional: for CMC API (if using requests instead of urllib)
# requests>=2.28.0

# Testing
pytest>=7.0.0
